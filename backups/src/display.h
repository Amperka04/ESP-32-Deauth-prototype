#ifndef DISPLAY_H
#define DISPLAY_H

#include <TFT_eSPI.h>

/*
 * Заголовочный файл для работы с дисплеем.
 * Объявляет глобальный объект tft и функцию инициализации.
 */

// Глобальный объект для управления дисплеем (доступен из любого места)
extern TFT_eSPI tft;

// Функция инициализации дисплея (вызывается один раз в setup)
void initDisplay();

#endif