#ifndef DISPLAY_H
#define DISPLAY_H

#include <TFT_eSPI.h>

/*
 * ====================================================================
 *  Модуль управления встроенным TFT-дисплеем (ST7789, 170x320)
 *  Плата Lilygo T-Display S3.
 *  Предоставляет глобальный объект tft и функцию инициализации.
 * ====================================================================
 */

// Глобальный объект для управления дисплеем (доступен из любого места)
extern TFT_eSPI tft;

// Функция инициализации дисплея (вызывается один раз в setup)
void initDisplay();

#endif